apiVersion: v1
kind: Service
metadata:
  name: {{ include "asr-service.fullname" . }}
  labels:
    {{- include "asr-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "asr-service.selectorLabels" . | nindent 4 }}
