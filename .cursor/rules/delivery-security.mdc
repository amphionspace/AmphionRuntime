---
description: 所有客户交付包的标准安全流程
alwaysApply: true
---

# 通用交付安全流程

当用户要求生成、更新、验收或发布任何客户交付包时，除非用户明确改变目标，否则必须遵守以下流程：

1. 只从干净检出构建正式交付包；不要从用户当前脏工作区直接打正式包。确需本地预览时，必须在产物元数据和最终回复里明确标注。
2. `.secure/`、keystore、私钥、签名密码、客户 SN、签发出的正式 license 一律按敏感输入处理；只读取必要内容，不在对话或客户文档中打印密钥、密码、完整 license payload 或完整设备哈希。
3. 客户可见文档不得暴露内部路径、仓库 URL、非公开分支、构建脚本、`.secure`、密钥配置、密码、TODO/FIXME，或把离线授权描述成联网鉴权。
4. 最终 zip 是唯一验收对象。设备安装、AAR/APK 内容检查、license claims、源码包验证和报告生成都必须从最终 zip 解压目录开始，不得用项目 `build/` 产物或手工回写目录替代。
5. Android APK 进入客户 zip 前必须可安装且签名可验证。签名来源必须进入版本元数据或验证报告；unsigned APK 不得作为正式交付物。
6. zip 内不得包含 `.git`、`.secure`、keystore、私钥、签名证书请求、构建缓存、临时产物、系统垃圾文件或客户不可见内部文档。
7. 打包脚本必须输出可复核版本元数据，至少包含产品版本、源码 commit、dirty 状态、构建日期、关键产物名和关键签名 / 授权口径。
8. 交付前必须运行静态验包：zip CRC、UTF-8 文件名标志、禁止文件名、AAR/APK 必需条目、开源 NOTICE、版本元数据和 license claims。
9. 如做设备验证，必须安装最终 zip 解压出的 APK；如做源码包验证，必须从最终 zip 解压出的源码工程运行。
10. 验证必须生成机器可读报告和简短 Markdown 报告，默认使用 `tools/delivery/verify_delivery_zip_e2e.sh`，并把报告路径、zip SHA-256、源码 commit、授权有效期和偏离标准流程的地方写入最终回复。
11. 交付 Markdown 文档的表格单元格只放原文，不使用粗体、斜体或删除线。
