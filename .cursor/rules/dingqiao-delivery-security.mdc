---
description: 鼎桥 ASR 客户交付的标准安全流程
alwaysApply: true
---

# 鼎桥交付附加约束

当用户要求打包或更新鼎桥 ASR 客户交付包时，必须先遵守通用交付安全流程（`.cursor/rules/delivery-security.mdc`），并额外遵守以下客户口径：

1. Demo APK 内置 license 仅用于体验：绑定 `com.amphion.dingqiao.demo` 和 Demo 签名，限制期限，不绑定 SN 清单。普通安装 Demo 必须能通过 `createEngine`。
2. 面向正式宿主 `com.tdtech.tiassistant` 时，用 `.secure/amphion-license-private.pem` 单独签发 `license/amphion-license.lic`；必须绑定客户 SN 白名单；鼎桥 ASR/TTS 共用交付时 `features` 使用 `ASR,TTS`。
3. 设备绑定规则必须是 `SHA-256(normalizedSn + deviceIdSaltId)`，其中 `normalizedSn` 为 trim 后转大写。默认 `deviceIdSaltId` 为 `DQ-TIASSISTANT-20260623-69CD375699165832C1D2E9EA77C8BE71`。
4. 声纹模型 `eres2net.onnx` 必须内置在 `dingqiao-asr-v*.aar` 和 Demo APK 的 `assets/amphion-dingqiao/` 下；客户包不得再要求外置 `models/eres2net.onnx`。
5. 文档必须清楚覆盖全部公开集成面：`init`、`setWorkPath`、`createEngine`、`setListener`、`startListening`、`writeAudio`、`finish`、`cancel`、`isBusy`、`shutdown`、`registerVoiceprint`、`deleteVoiceprint`、`setLicense`、`getLicenseInfo`、监听回调、数据结构、错误码、热词、声纹模型自动准备、授权绑定和最小调用顺序。
6. zip-only 验证必须反查 Demo license claims：`applicationId=com.amphion.dingqiao.demo`、`features=ASR`、`device_hash_count=0`、`expiresAt` 非空；正式 license 单独反查 `features=ASR,TTS`、SN 白名单数量和到期时间。
