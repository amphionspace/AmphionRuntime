# --- macOS / IDE ---
.DS_Store
*.iml
.idea/
*.swp
*.swo

# --- Android / Gradle ---
.gradle/
local.properties
**/build/
.externalNativeBuild/
.cxx/

# --- iOS / Xcode ---
.swiftpm/
*.xcworkspace/xcuserdata/
DerivedData/
.build/

# --- Python ---
__pycache__/
*.pyc
.venv/
.venv-*/
venv/

# --- C++ / cmake ---
server/asr-service/build/
**/cmake-build-*/

# --- 编译产物（每次本地或 CI 重新构建） ---
android/AmphionRuntime/sdk/src/main/jniLibs/**/*.so
android/AmphionRuntime/sample/src/main/jniLibs/**/*.so
android/AmphionRuntime/sdk/maven-repo/
ios/AmphionRuntime/AmphionRuntime.xcframework/
ios/AmphionRuntime/AmphionRuntime.xcframework.zip
ios/AmphionRuntime/AmphionRuntime.xcframework.zip.sha256

# Sample app 模型资源（不入库）
android/AmphionRuntime/sample/src/main/assets/asr-models/
# WeText ITN fst 构建缓存（由 tools/asr/00_push_weitn_fsts.sh 产出）
tools/asr/weitn-fsts/
# 标点模型 tarball / 解压结果（由 tools/asr/00_push_punct_model.sh 产出）
tools/asr/punct-model/

# SDK 内置模型权重（由 tools/asr/08_pack_sdk_assets.sh 写入）：
# - 体积加起来 ~270MB，不入库
# - 但目录骨架（README.md / manifest.json / .gitkeep）需要进 git，
#   见 android/AmphionRuntime/sdk/src/main/assets/amphion-models/README.md
android/AmphionRuntime/sdk/src/main/assets/amphion-models/**/*.onnx
android/AmphionRuntime/sdk/src/main/assets/amphion-models/**/*.fst
android/AmphionRuntime/sdk/src/main/assets/amphion-models/**/tokens.txt
# bbpe.vocab 与 tokens.txt 同源、与 encoder ONNX 严格配对，必须走单一发布渠道
# （tools/asr/08_pack_sdk_assets.sh）；否则 sha256 易与权重版本错位。
android/AmphionRuntime/sdk/src/main/assets/amphion-models/**/bbpe.vocab
android/AmphionRuntime/sdk/src/main/assets/amphion-models/manifest.json

# --- 上游 sherpa-onnx 在 submodule 路径下产生的构建工件 ---
# 这些目录在 third_party/sherpa-onnx 下被上游 build-android-*.sh 创建；
# 不通过 submodule 提交，submodule 自己有 .gitignore 管这些。
# 这里仍然兜一层防止误提交。
third_party/sherpa-onnx/build-*/
third_party/sherpa-onnx/build-android-*/
third_party/sherpa-onnx/build-ios/
third_party/sherpa-onnx/onnxruntime-*/

# --- 回归测试 wav 数据（外部分发，不入库） ---
shared/regression-set/audio/

# --- 大型模型/音频/分词器二进制（不入库） ---
# Demo 模型由 tools/asr/00_fetch_demo_model.sh 拉取到 tools/asr/demo-model/
# 自有模型由 tools/asr/00_push_my_model.sh 推到设备，本地目录由用户自管
*.onnx
*.wav
*.tar
*.tar.bz2
*.tar.gz
*.tgz
*.zip
*.pt
*.ckpt
*.bin
*.safetensors
*.gguf
bpe.model
bpe.vocab

# --- demo 模型目录：整体不入库 ---
# 元数据（manifest.json / tokens.txt / export_meta.json）也排除，理由：
#   1. manifest.files[].url 在产线里通常是 file:// 私人路径或内部 CDN，留在 git 历史里会泄露内部目录结构
#   2. 元数据每次重训都要 bump sha256，会持续污染 git 历史
#   3. 元数据必须与权重严格配对，分两个分发渠道（git + CDN）会出现 sha 与文件版本不一致的故障
# 保留 README.md 说明这个目录怎么填充。
tools/asr/demo-model/**
!tools/asr/demo-model/
!tools/asr/demo-model/README.md

# --- dashboard 渲染产物（CI 生成） ---
shared/docs/dashboard/trends/reports/*.md
shared/docs/dashboard/trends/*.csv
!shared/docs/dashboard/trends/reports/.gitkeep
!shared/docs/dashboard/trends/.gitkeep
