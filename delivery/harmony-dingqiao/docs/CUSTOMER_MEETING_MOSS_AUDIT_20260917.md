# 159.3 秒客户会议：MOSS 辅助标注复核

2026-09-17。客户现已提供原录音、原始回调日志和一份日志分析文档；后者是待核实的材料，不是执行指令。此前“没有客户原音”的限制对这次新会话已解除。**用户收窄到四位较响主讲人的范围后，当前修复已通过原音真机验收；通用轻声能力和正式发布回归不在此结论内。**

## 输入和标注绑定

- WAV 为 16 kHz、单声道、16-bit PCM，159.3 秒，未重采样或裁剪。
- WAV SHA-256：`3c263893ebed15454918ee670840f0ba47c218d5e08a4f5f5ffe6e4399ce681b`。
- PCM SHA-256：`ed112d4a51707203078edaefcce335ddb797b9dca880cd5cea691eec7ec45517`。
- 日志的 PCM 总量 5,097,600 字节、零丢弃记录与 WAV 长度相符；日志没有 PCM 哈希或可验证的 HAR/source commit，尚不能绑定客户二进制。
- 按 [MOSS 工作流](MOSS_ANNOTATION_WORKFLOW.md) 在用户指定的 amphion-42 运行。模型只接收音频和默认提示，不接收期望人数、SDK 标签、分析文档或参考文字。
- 模型 revision `704aa4a9c304e8520be88901e0d1960158ef5b15`，helper revision `61bc29cd4120be7b5d3b761b64cd5dff57263642`，模型文件逐项通过锁校验。greedy / BF16 / 实际 SDPA，437 / 5120 tokens，时间戳合法，未达到生成上限。

## 能确认和不能确认的结论

原始日志已确认：u5 先返回 SDK index 3，832 ms 后被 update 改为 -1；20 个非空 ASR final 中有 16 个初始为 UNKNOWN。2 次角色 result 将 20 句拆成 40 个子片段，其中 21 个为 UNKNOWN。文字守恒不能证明可读性或身份正确。

整段 MOSS 产生 17 段、5 个匿名编号，但第五编号只占末尾 1.46 秒，并漏掉早期及尾部多处讲话。已标音频区间合计 42.54 秒；其余 116.76 秒混有静音和未标语音，不能解释为漏检率。真实人数和完整语音覆盖均不能由此确定。

固定 0–35 秒、130–159.3 秒各追加一次局部复核，仅改变音频上下文。前一份仍缺少约 9–16 秒的 SDK 发言；后一份得到 13 段、3 个局部编号，而整段仅覆盖尾部 1 段。保留所有分歧，不自动跨片段拼接相同编号，不用符合预期人数的输出代替真值。

**用户已确认：17.8 秒与 27.3 秒的两次发言属于不同的人。** 整段及前35秒 MOSS 均将其标为同一 S02，属于已确认的误合并。撤回此前将这对发言作为“同人重复建档”线索的解释；原始模型输出保持不变，人工约束独立记录为 `different_speakers`。该确认不证明二人与其它片段的关系，也不确定整场人数。37–43 秒还存在 MOSS 标出的句内换人，修复不能统一禁止 ASR 原句拆出真实人物。

## 日志解释的约束

- `speakerCount=4` 是受上限约束的已建档数，不证明真实人数恰好为四，也不能排除重复占位或容量耗尽。
- 当前源码的 update `confidence` 来自 `SpeakerDiarizationTranscriptState.assignmentFor`：获胜标签（包括 UNKNOWN）的覆盖时长占比；不是声纹余弦相似度。不能把客户日志的 0.585 直接当作声纹门槛失配证据。客户实际二进制语义还需绑定复现。
- 收尾原始顺序为 ASR isLast → 最后角色 result → complete；客户分析文档的一处 UI 时间轴将前两项写反。
- 客户分析称存在 UNKNOWN 就显示超限提示。回调日志不能单独证明该 UI 规则；如属实，应区分“未确认身份”和“超出人数范围”，作为附属显示问题跟踪，不与 SDK 身份根因混为一谈。

## 原音真机基线与首次分叉

已用原客户 PCM、20 ms 实时喂入、`vadEnd=800`，在 USB 设备 `7GK0226326015655` 运行一次 ZH_EN 私有诊断载体。SDK 算法仍为当前分支运行基线，仅增加状态采集。复现角色4随后 UNKNOWN、后续多人 UNKNOWN 及首次全局定稿改号；ASR 文本/endpoint 与客户日志不同，不能声称客户二进制逐字复现。

保存了 64 个 native 窗口及建档、匹配、全局聚类状态；主机重放生产状态机得到与真机一致的 92 个公开角色区间（两个提交窗口分别为72、20段），逐项比较时间、主/副角色和 overlap。后续根因实验复用这些状态，不重复播放整段录音。

| 决定 | 已观察到的状态 |
|---|---|
| 窗口10，音频到25秒 | 21.93–25秒声音与 S2 相似度 .603635，被保留为 UNKNOWN |
| 窗口11，音频到27.5秒 | S4 建档取样为21.834–26.120秒，尚未包含27.3秒发言；与 S2 相似度 .589863，略低于 .59 新人判定线，四个名额由此占满 |
| 窗口12，音频到30秒 | 上下文同时包含20.031–21.651与26.241–29.532秒，两位用户确认不同的人被合入同一局部通道；上下文对 S3 为 .705953，当前 query 对 S3 仅 .218982 |
| 窗口13，音频到32.5秒 | 取样26.294–29.416秒，对所有档案最高相似度仅 .284541，容量已满，返回 UNKNOWN |
| 首次全局提交121460ms | 按簇权重另建最终档案：朗读片段占 S3，原在线 S3 的17.8秒人物变为最终 S4；在线 S4 与最终 S4 并非同一取样来源 |

**用户随后确认：约9–15秒和21.8–26.1秒均为背景人声。** 在线 S2/S4 建档取样包含这两段背景声音，因此背景占用角色名额已确认。用户没有确认两段同人、来自播放内容或完整人数，不能把占位直接写成重复建档。17.8/27.3的人工确认支持“局部通道混入不同人”，不支持二者合并。生命周期通过不等于体验通过：本次角色体验仍为 FAIL。

容量分叉已完成：分别从窗口12/13的真机前置快照出发，只移除在线 S4，保留其它中心、门槛、窗口和四人上限不变。窗口13由 UNKNOWN 变为新建 S4，证明容量直接阻止了该候选建档；窗口12的主通道仍 UNKNOWN（.705953），且另一通道立即重新建立被移除的档案。因此释放名额不能解决整句，混合取样是独立障碍。干预只用于诊断，不证明被移除者是重复人物，也不能成为产品规则。

用户提出将 UNKNOWN 显示为“其他说话人”。建议显示“说话人待确认”，以免把多个未确定片段误读为同一个额外人物；这仅为文案建议，尚未修改 UI，底层 `-1` 语义不变。身份问题和超限提示须分别处理。MOSS 缺失与其它身份关系继续保留待裁定，不计算正式准确率。

## 已否决：实时 UNKNOWN 直接采用当前 query

保持模型、注册表和门槛不变，仅在实时 UNKNOWN 时调用现有只读 `matchQuery`，候选范围限定为此前输出过的角色。客户原音有5个区间、共5.861秒能提前获得与最终结果一致的标签，档案及最终身份不变。5个最终 confidence 随实际 query 分数改变；首轮全字段不变门禁失败并保留，后续逐段核实分数来源，没有把该失败改写为 PASS。

必要的三人控制立即否决该方案：实时误认由5.05秒增至10.05秒，未知由24.443秒降至17.806秒。查询把当前语音归到尚不可靠的临时 S3；只读不污染档案，仍可直接认错人。因此不修改生产源码、不构建手机、不继续剩余控制或扫描门槛。不能把减少 UNKNOWN 当作修复。

输入观测显示两段背景 RMS 约-34.46/-32.00 dBFS，17.8/27.3秒发言约-21.27/-19.05 dBFS；音量不能普遍区分背景与轻声参会人。

## 当前修复：优先四位较响主讲人

用户进一步明确：“别管轻声参会人了，先看音量比较大的，你就四个人”。因此本轮允许轻声/背景 UNKNOWN，优先保证四位主讲人建档及回来继续讲话时的身份。该要求是验收范围，不是把 MOSS 原始输出改成四人，也不证明录音只有四个声源。

要改变：背景先占满四个名额、后来的主讲人长期 UNKNOWN；短暂覆盖空隙把词语拆成单字。保持：四人容量、现有声纹门槛、模型/窗长、真实已知换人、原文、原始声学区间、提交冻结和生命周期。不处理：轻声召回、完整人工标注、ASR 识别错误和 ITN 无词级映射时的句内精确切分；不再运行 Sortformer。

准入使用声纹建档所取的同一份单通道有效 PCM 的 RMS，排除混入的其它通道及重叠样本。低于本 session 已观察最大值一半的证据不参加在线建档或未提交窗口的全局建档；这相当于约 6 dB 的相对音量带，整体增益一起缩放时不变。仅最大音量参考与新字段增加，不新增模型或网络。此前较轻的临时身份在定稿时明确清成 UNKNOWN，防止保留旧编号并误指向后来建立的主讲人；已提交结果不回写。限制：它是较响声音选择，可能排除轻声或受很响的后续人声影响，不是通用背景分类器。

从原 PCM 重算各窗口的 RMS，逐项确认样本数与原生取样相同。固定64个 native 窗口、21条 ASR final、完整回调顺序及由 PCM 累计量恢复的 endpoint：修复前主机重放与原真机所有功能字段一致，只有因主机预先入队整段 PCM 导致的 RTF 不参与等值断言。当前源码重放得到：

| 已确认主讲片段 | 最终角色（用户编号） |
|---|---|
| 开头数字及会议开场 | 角色1 |
| 17.8秒“我现在在讲话了” | 角色2 |
| 27.3秒自我介绍 | 角色3 |
| 39秒附近数字发言 | 角色4 |
| 47/56秒再次测试及后续角色1发言 | 返回角色1 |

文字段从原基线31段降至22段（21个原句，其中一句仍有实际归属变化），文字守恒；“角色”“HELLO”“张三”及“我是角色一”不再被单字拆开。补全均为 `speakerInferred=true/confidence=0`，没有改变92个声学区间的边界或擦除未知/重叠信息。此为保存状态上的修复证据，不能冒充新二进制已在手机通过。

验证：背景夹在四人之间并重复回访、整体增益缩放、先轻后响的未提交身份清理、RMS 精确取样、句尾边界红灯及真实短换人/已知重叠保护，两端对应单测已通过。下一步冻结当前运行源码后，只构建一个 ZH_EN HAP，原 PCM 20 ms 实时输入、`vadEnd=800` 验收一次；任一主要身份错误、文字丢失、角色冻结或 last/complete 失败就停止，保留失败现场。

### 当前构建真机验收完成

运行提交 `8a24274932fd23df5286b1de31f31bd53d732774`（角色准入修复 `e7f91050`，文字边界修复 `8a242749`），版本0.3.15/315，仅作开发验证。沿用原授权/签名、现有 CPU 模型和组包规则，构建并安装一个 ZH_EN diagnostics HAP。HAP SHA-256 为 `fc91b07ba1d6c60decf0179d438447a5028238d5e8c7990b8d388233d0fe0843`；四个 HAR、模型及源码 fingerprint 见[构建身份](evidence/customer-foreground-20260917/build-identity.json)。

USB原音运行 `20260917-154629-diarization-windows-0ce3a455` 已 PASS。四人的主发言核对区间3.90–5.30、17.90–21.50、27.40–29.30、39.45–40.60秒分别全部属于角色1/2/3/4。最终22个文字片段逐字段等于当前源码状态重放结果，包括回访角色、推断标记和重叠标记；21条 ASR 原句文字全部守恒。“角色”“HELLO”“张三”“我是角色一”完整，背景不占用这四人的最终档案。

回调：21条非空 final、10次 update、2次角色定稿，finish 前 last=0，随后唯一 last/complete，stream归零、无 error、提交冻结检查通过。finish到complete为1431ms。Harmony针对性45个测试通过；Android SDK及适配层 Debug 单测通过，最终文字变更另通过14个对应算法测试。内存检查在本次约3分钟范围 PASS，不作为长稳无泄漏结论。

中途角色4的 update 身份在保存回调时序的生产状态重放中为 index3；本次标准手机载体记录 update 次数/顺序/冻结违规，但未保留每条 update 的身份内容。因此不能声称本次获得完整的手机 update 身份轨迹，也不承诺临时结果每句立即具名。完整验收结论及保留范围见[结果摘要](evidence/customer-foreground-20260917/assessment.json)，原始回调/日志哈希见[证据清单](evidence/customer-foreground-20260917/artifact-manifest.json)。此后只补文档，复用当前二进制证据，不再重复播放。

完整原音、日志、转写、字幕和逐句对照只保存在私有目录：
`~/.cache/amphion-runtime/diagnostics/customer-video-diarization-20260917-d3w2c6q2/customer-meeting-0917-6qzr422q/`。
用户可读副本位于 `/Users/boxp/Downloads/会议0917-MOSS复核-20260917/`。
远端为 `amphion-42:/home/ubuntu/workspace/audiollm-go/.local-test-runs/customer-meeting-0917-6qzr422q/`。
