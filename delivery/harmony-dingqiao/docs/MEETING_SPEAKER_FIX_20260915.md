# 会议说话人稳定性：采样与交接片段归属修复

**两项候选修复已通过四条音频真机对照，原 COS 会议 DER 从 71.34% 降至 26.56%，但整体身份稳定性仍未通过，尚不能关闭缺陷。** 核心结束回调契约通过；一个输入没有覆盖中途窗口。通用 64 MiB 压测门槛原始结果为 FAIL，但尚未证明适用于会议角色分离的资源预算，不能据此判定泄漏或单独否决修复。

## 第二阶段：用输出片段核对已建立的角色

运行代码提交 `fd6211230a635c6b6b64fab20b0827aecb254718`。已完成同一鸿蒙真机上四条共 12 分钟音频的公共 SDK 回放；四条公开身份区间与宿主机原型逐段一致，以下改善已由新 HAP 复现。

**根因：建档上下文和当前输出片段可能属于不同人。** 原会议 93.5–96 秒已换回说话人 `006-M`，但该窗口 6 秒采样主要来自上一位 `004-M`。只用当前输出区约 1.996 秒真实单人 PCM 查询，返回发言者相似度为 0.6296，错误角色为 0.4058。

- 改变：额外提取当前 job 输出区内至少 1 秒的真实单人 PCM，用于核对已经命名的片段；候选只包括之前已公开的角色和当前窗口原聚类实际输出的角色。
- 保持：6 秒建档采样、建档/全局聚类门槛 0.72、匹配差值 0.05、原模型、公开窗口边界、已发布结果冻结、PCM 归属和结束回调语义。查询不创建/更新角色档案，不把 UNKNOWN 强行命名；不足 1 秒不借上下文或补静音。
- 不处理：换模型、修改角色上限、身份建档策略、重叠语音解混及正式发布。

查询门槛 0.59 来自独立的 24 人 AISHELL3 校准：最大异人相似度 0.539121，加既有差值 0.05 后向上取整至百分位。会议身份标注未参与门槛选择；这个有限校准不代表所有短句都能可靠匹配。

首次查询原型允许匹配所有登记档案，在双人录音中误用了一个只在上下文存在、从未实际输出的第三人档案，串人从 0 增至 1.92 秒，因此否决。限制候选范围后，原会议两处交接的身份混淆合计减少 4.30 秒（13.84 → 9.54 秒），DER 29.76% → 26.56%；其他三条逐段身份保持一致。四条档案向量和公开时间边界与原方案完全相同。原会议仍有 5 次已命名变号和 35.13 秒 UNKNOWN，尚未解决全部身份稳定性问题。

回归包含短片段纠错、不更新建档、排除上下文虚假角色、严格 PCM 区间、次秒不借上下文、置信度随结果冻结，以及实际会话提交。旧会话在相同交接输入下稳定返回错误的编号 1；修复返回编号 0。Harmony 共 33 项相关检查通过，Android 两个 SDK 的 Debug / Release 各 156 项通过。[诊断与校准摘要](evidence/meeting-speaker-fix-20260915/core-query-diagnostics.json)。

### 第二阶段真机结果（16:41 完成）

| 录音 | DER：6 秒上一版 → 本轮 | 身份混淆秒：上一版 → 本轮 | 本轮 UNKNOWN 秒 | 本轮已命名变号 |
| --- | ---: | ---: | ---: | ---: |
| 原 COS 会议 | 29.76% → 26.56% | 13.84 → 9.54 | 35.13 | 5 |
| `M_R003S01C01` | 5.85% → 5.85% | 3.41 → 3.41 | 2.91 | 4 |
| `S_R004S01C01` | 9.40% → 9.40% | 6.29 → 6.29 | 7.14 | 1 |
| `S_R004S03C01` | 0.94% → 0.94% | 0.00 → 0.00 | 0.00 | 0 |

四轮核心生命周期 PASS：每轮 start=1、finish 前 last=0、唯一 last 后唯一 complete、error=0、native stream=0；分句数量 18 / 13 / 12 / 14 与基线一致。中途窗口专项仍为 3/4 PASS：`S_R004S03C01` 与旧版一样只有终场窗口，保留原始 FAIL。

额外查询增加计算代价。按主讲、四人、双人、COS 的顺序，RTF 为 0.431 / 0.494 / 0.675 / 0.933，上一版为 0.347 / 0.392 / 0.584 / 0.696；finish 到 complete 为 3.216 / 4.586 / 7.553 / 14.537 秒。不能把目前 RTF 小于 1 推广为所有长会话或设备都能实时处理。

本轮整个进程 RSS 峰值 973.328 MiB、头尾增量 125.867 MiB，线程增量为 0。默认 64 MiB 检查仍为 FAIL；会议资源预算与泄漏结论仍为 INCONCLUSIVE。单轮峰值低于上一版不构成资源优化证据。

- HAP SHA-256：`d18d6c0d557919765524be6453160849a55584b11c1cc4790b580ade9c4830e4`。
- 源码指纹：`96f40cbc96f60cd2f311e802bc6dd32b92549779e1f0a5a93d6cd9fe016df73b`；四个 HAR 与 HAP 已通过源码一致性复核。后续只补回归测试和证据，不改运行代码。
- 原报告：`usb-core-query/20260915-162746-diarization-windows-33d08e59/report.json`，SHA-256 `e1134705f5da493c5c18033212959a327d73495706db7761a3a66e5588d4111c`。
- [当前逐条评估](evidence/meeting-speaker-fix-20260915/core-query-device-assessment.json)、[当前构建身份](evidence/meeting-speaker-fix-20260915/core-query-build-identity.json)、[完整回调与资源证据清单](evidence/meeting-speaker-fix-20260915/core-query-evidence-manifest.json)。

## 第一阶段：6 秒采样改动

Harmony 与 Android 的 `SpeakerDiarizationInference` 在 10 秒推理窗口内，按原顺序选取该通道最多 6 秒的有效单人语音，替换原先的 2.5 秒上限。其他通道、重叠 mask 和无声区域仍不进入该声纹；按原时间顺序拼接有效 PCM。原会议第 111 秒的连续发言变号，是本次根因回归的验收对象。

- 改变：后段已确认单人语音参与当前窗口的身份判断。
- 保持：匹配门槛 `0.72`、匹配差值 `0.05`、1 秒 embedding 起算条件、角色上限、10 秒窗口 / 2.5 秒步长 / 1.5 秒右上下文、公开窗口冻结、纯端侧推理、公共 API、ASR PCM 归属及 last/complete/cancel 语义。
- 不处理：分割模型替换、重叠语音解混、ASR 精度、UI、正式打包发布。

采样修复提交 `20aa0ca8`、资源约束提交 `a9df59dc`；第一阶段验收源码冻结在 `a9df59dc31de15dae0f7c3c5f339af1a700b23bd`。阈值实验提交 `82df67d5` 已撤回，不属于最终实现。没有推送或合入 main。

## 根因证据与否决实验

[原始问题报告](MEETING_SPEAKER_STABILITY_20260915.md) 保存了 COS 会议的真机失败现场。分割通道混入前一位说话人后，最前 2.5 秒的声纹输入无法代表后面的发言；相邻窗口因取样移动形成不同身份。固定 native 分割、匹配参数与公开窗口边界，只改为完整单人语音采样，原第 111 秒变号消失。完整采样不能保证分割通道本身绝对纯净，因此不代表所有身份误判已经解决。

原会议及新增三条会议的宿主机基线，分别在 18,000 个 10 ms 主说话人帧上与对应真机结果完全一致。宿主机用于同输入状态差分，仍标记 non-canonical，不能代替最终 HAP 验收。

| 原会议状态差分 | 250 ms 边界宽限、排除重叠 DER | 身份混淆秒数（完整区域） | UNKNOWN 秒数 |
| --- | ---: | ---: | ---: |
| 前 2.5 秒、0.72（基线） | 71.34% | 13.00 | 96.97 |
| 完整窗口、0.72（10 秒中间方案） | 29.76% | 13.84 | 35.13 |
| 完整窗口、0.60（已否决） | 14.34% | 16.17 | 7.70 |

`0.60` 来自另一候选分支的独立 AISHELL3 短句校准，但在本会议上增加了身份混淆，违反实验前规定的“不能增加异人混淆”判据，因此撤回。只取靠近输出区的 2.5 秒、禁止同窗口不同本地通道合簇也未通过诊断对照，未引入产品代码。失败现场及否决理由均保留。

UNKNOWN 会隐藏原本无法判断的身份错误，变号次数必须和有效身份覆盖、身份混淆一起阅读。DER 为说话人分离错误率，不是 ASR 字错率；UNKNOWN 秒数使用完整输出语音段口径，与排除边界后的统计范围不同。

## 测试音频

除原 COS 会议外，从 [AISHELL 官方 AISHELL-4 数据集](https://huggingface.co/datasets/AISHELL/AISHELL-4/tree/main/test) 取得三场录音和 RTTM。查看模型输出前，按标注选择起点：30 秒网格、起点不早于 180 秒、不超过 4 位参考人、至少 2 人各有 10 秒语音。全部使用原第 0 通道、180 秒、16 kHz PCM16，不重采样、不拼接。

| 录音 | 原文件区间 | 参考人数 | 场景 |
| --- | --- | ---: | --- |
| `20200623_S_R001S01C01`（COS） | 0–180 秒 | 4 | 原始连续发言变号 |
| `S_R004S01C01` | 180–360 秒 | 4 | 多人轮流发言，第四人仅插话 0.90 秒 |
| `S_R004S03C01` | 180–360 秒 | 2 | 双人交替 |
| `M_R003S01C01` | 210–390 秒 | 2 | 主讲与返回发言 |

同一 PSN-AL00 / OpenHarmony-6.0.2.130、同一模型和参数、20 ms 实时输入，比较各自公共 SDK 结果。三条新增音频及标注的来源、截取条件和 SHA-256 保存在证据清单。

## 第一阶段构建与单测

- 当前验收源码：`a9df59dc31de15dae0f7c3c5f339af1a700b23bd`。
- HAP SHA-256：`b1f81460b46dc3a08a3786a580d02df3d62249e5b5a25f252f1aada07985c5ea`。
- 源码指纹：`ce0a1f495c92a766ba559ec793a2c5db7e6395151971e564f495493aab852f2a`；四个 HAR、模型、native 哈希随构建身份保留。
- 仅构建一个 ZH_EN diagnostics 测试载体；SDK 0.3.13、carrier 0.3.14 / 314，不调整产品版本。
- 原默认开发证书于本日 11:47 过期。使用本机另一套有效的同应用开发签名，完整通过签名、profile、授权、模型检查和覆盖安装；没有绕过检查。新签名有效至本日 16:44:58，与推理结果无关，但之后重装需有效签名。
- 修改前采样回归红灯：应保留的 96000 个单人样本仅留下 40000 个；修改后通过，并核对其他人、重叠排除、PCM 顺序和尾部样本。
- 最终 Harmony 相关状态机与评估器 30 项通过；Android SDK / 鼎桥 SDK 的 Debug、Release 各 153 项通过。包括公开窗口冻结、不同已定稿身份不合并、UNKNOWN 与跨边界归属；未修改模块的有效 Gradle 任务复用。

## 资源问题与有界采样

10 秒中间方案的四条真机回放已完成：原 111 秒已命名身份跳变消失，四条身份评估均改善，但通用内存增长检查 FAIL，RSS 增量 138.17 MiB 超过脚本默认 64 MiB 门槛，峰值 1045.262 MiB，RTF 为 0.465–0.866。该方案与原始报告保留；身份稳定性仍未通过，不能交付。不能单独用这个通用资源门槛否决会议方案。

不能据此宣称持续泄漏：对相同前三条录音的局部观测，RSS 增量为 38.45 MiB；旧包三条为 61.07 MiB。这个裁剪结果只是定位证据，不能替换完整四条的失败报告。结束后的同 PID 资源也有回落，但未据此改写工作负载门禁。

固定原会议 73 个窗口，在新进程分别运行两种分配策略：禁用 CPU arena 后，48640 个 embedding 分量完全相同，但宿主机峰值及驻留内存反而更高，因此未改变分配策略。

原异常通道有 5.956875 秒有效单人 mask；6 秒是保留这份完整证据的最小整秒上限。改用 6 秒后，同样四条录音的 72000 个主说话人帧与 10 秒真机结果逐帧一致。宿主机原会议峰值从 2075 MiB 降至 1577 MiB，计算加采样观测耗时从 10.02 秒降至 7.45 秒。宿主机内存分配器不同于鸿蒙，以上仍是 non-canonical 诊断，不能代替真机资源验收。

## 第一阶段真机结果

6 秒候选已经完成四条共 12 分钟真实音频的同条件 USB 对照。以下 DER 使用 250 ms 边界宽限、排除参考重叠；变号统计只看单人区域内的已命名 SDK 编号。

| 录音 | DER：旧包 → 候选 | UNKNOWN 秒：旧包 → 候选 | 已命名变号：旧包 → 候选 |
| --- | ---: | ---: | ---: |
| 原 COS 会议 | 71.34% → 29.76% | 96.97 → 35.13 | 3 → 5 |
| `M_R003S01C01` | 36.65% → 5.85% | 43.52 → 2.91 | 4 → 4 |
| `S_R004S01C01` | 25.70% → 9.40% | 26.88 → 7.14 | 1 → 1 |
| `S_R004S03C01` | 23.85% → 0.94% | 29.94 → 0.00 | 3 → 0 |

原 COS 会议 `001-M` 的已命名编号由原来的 `3 → 1` 变为同一个 `2`，第 111 秒不再变号；但该句仍有未知身份片段，**整句身份覆盖没有通过**。另两位参考人仍被分成不同编号；UNKNOWN 变少后也会显现原先被隐藏的编号不一致。原会议身份混淆从 13.00 秒变为 13.84 秒，不能仅凭更低 DER 宣称全部身份更准确。

| 检查 | 结果 |
| --- | --- |
| 核心生命周期 | 4/4 PASS；每轮 start=1、finish 前 last=0、结束后唯一 last → 唯一 complete、error=0、native live stream=0 |
| 分句数量 | 18 / 13 / 12 / 14，与各自基线相同 |
| 中途窗口专项 | 3/4 PASS；双人录音与基线一样只给出终场窗口，原载体仍 FAIL |
| 整体身份稳定性 | FAIL；已命名变号总数 11 → 10，仍存在身份混淆与未知片段 |
| 通用资源检查 / 会议适用性 | 原始检查 FAIL：RSS 增量 **118.754 MiB**，默认 **64 MiB**，线程增量为 0；会议资源预算与泄漏结论 INCONCLUSIVE |
| RSS 峰值 | 988.184 MiB；10 秒中间方案为 1045.262 MiB |
| 声纹分离 RTF | 0.347 / 0.392 / 0.584 / 0.696；10 秒中间方案为 0.465 / 0.489 / 0.750 / 0.866 |
| finish 到 complete | 3.123 / 4.429 / 7.291 / 11.847 秒；依次为主讲、四人、双人、COS 会议 |

RTF 表示累计分人推理时间 / 音频时长。虽然均小于 1，计算代价仍高于旧包；没有用该指标代替资源或生命周期门禁。

### 未解决项与处理状态

1. **身份误判 OPEN**：原连续发言中的已命名变号已消除，其余短插话、混合本地通道和少数人的身份仍有错误，尚未逐条证明最早分叉。不能关闭整体角色稳定性缺陷。
2. **会议资源预算 INCONCLUSIVE / 泄漏未证实**：已补相同四条连续会话的旧实现对照，旧版增长 95.283 MiB，新版 118.754 MiB，两者均超过通用 64 MiB。新版本的工作内存和计算开销更高，但增长本身不是泄漏证明；默认检查未按会议场景定标。原始门槛和 FAIL 记录保留，不把它直接作为身份修复的判废条件。
3. **窗口覆盖不足**：见下一节。它独立于采样改动，保留原始 FAIL，没有改动断言。

在 6 秒状态下，单独关闭 memory pattern 的宿主机驻留 RSS 从 1149 MiB 增至 1311 MiB；再关闭 arena 增至 1736 MiB。两种状态的 48640 个 embedding 分量均完全一致，但没有降低驻留内存，因此均否决，未引入分配策略配置。采样上限搜索及分配配置实验按既定停止条件结束，不再无假设重放。

本分支仅保留可审阅的候选与证据；不推送、不合入、不生成交付声明。

### 15:48 补充：同四条资源对照与门槛解释

在独立对照分支 `test/meeting-speaker-resource-control` 的 `a3d83b0b`，仅恢复两平台原 2.5 秒采样，重建相同签名与 diagnostics 载体，按相同顺序连续跑四条原 PCM。配置、设备、源音频及 PCM 哈希全部相同；四条旧版精度与原基线逐项一致，四轮核心生命周期均通过。完成后已切回候选分支，校验并重新安装原 6 秒 HAP，未重跑相同二进制验收。

| 整个测试进程 | 原 2.5 秒 | 当前 6 秒 |
| --- | ---: | ---: |
| 运行中头部 RSS 中位数 | 653.293 MiB | 703.574 MiB |
| 运行中尾部 RSS 中位数 | 748.576 MiB | 822.328 MiB |
| 头尾差 | 95.283 MiB | 118.754 MiB |
| 峰值 | 883.840 MiB | 988.184 MiB |
| 线程增长 | 0 | 0 |

差值显示候选增长量多 23.471 MiB、峰值多 104.344 MiB。旧版结束并 shutdown 后，同 PID RSS 降至 290.410 MiB、线程降至 41；这个快照不替代原始工作负载检查，也不足以单独证明零泄漏。

`memory_verdict` 排除前 20% 样本后，再比较剩余样本头尾各 20% 的 RSS 中位数。它测量整个进程，未隔离 SDK、ASR、模型分配器和保存测试证据的载体；头尾也不是同一音频、同一生命周期位置。因此 **64 MiB 是通用报警线，尚不是已定标的会议场景产品预算**。

缓存可按其职责增长：模型工作区可能上升至稳定高位；SDK 保留未提交窗口与跨窗口句子，提交后裁剪历史；PCM 写入 10 秒磁盘块，消费后删除。测试载体另保存整轮窗口、时间线及序列化记录。正确的专项资源验收应区分峰值预算、长会话稳定后的增长趋势、未提交证据大小、推理积压，以及相同负载结束后的残留；本次没有据此宣布资源 PASS，也没有修改或放宽全局检查。

[完整同条件对照](evidence/meeting-speaker-fix-20260915/four-session-control-assessment.json)和[新增原始证据清单](evidence/meeting-speaker-fix-20260915/continuation-evidence-manifest.json)保存了报告哈希、输入映射和各项指标。

### 剩余变号的内部状态及已否决方案

原会议约 93 秒换人时，`w00000038` 将 85.031–95.000 秒合为一个本地通道；选入身份模型的前 6 秒仍主要是前一人。`w00000039` 也在同一通道混入前后两人。后来的 `w00000041` 才把 93.31 秒后的新说话人分开。这是分割/声学证据的错误，不能通过外层固定编号处理。

固定四条音频的已保存状态，以下方案均标记 non-canonical 并否决，未进入产品：

- 相邻窗口的互相唯一重叠对应强制合簇：原会议串人增加；主讲录音已命名变号 4 → 7。
- 重叠窗口多数投票：原会议变号 5 → 7，混淆 13.84 → 14.02 秒。
- 选择有 7.5 秒后续上下文的窗口前段：混淆减少，但四条 DER 均变差、原会议 UNKNOWN 35.13 → 45.98 秒，且增加 6 秒延迟。
- 将 6 秒真实 PCM 分块提取再加权平均：第一条原会议混淆 13.84 → 15.68 秒，立即停止，未做多余真机重放。
- 采用后续窗口最新的已识别身份：原会议混淆 13.84 → 15.57 秒，变号 5 → 8；另一条也增加串人。

这些结果排除了以延时、投票或简单声纹平均代替分割/身份模型改进的方向。同架构 ERes2Net-base 的普通话 200k 训练版本也做了仅换权重的本地对照：原会议混淆 13.84 → 17.68 秒，变号仍为 5 次，未通过即停止，未替换 SDK 或既有声纹模型。ERes2NetV2 按独立数据校准为 0.67 后，原会议仍增加混淆至 19.40 秒、变号至 9 次，也已否决。模型来自 [sherpa-onnx 官方发布](https://github.com/k2-fsa/sherpa-onnx/releases/tag/speaker-recongition-models)，训练说明见 [3D-Speaker](https://github.com/modelscope/3D-Speaker)。[后续否决实验清单](evidence/meeting-speaker-fix-20260915/rejected-continuation-experiments.json)保存了摘要和原始文件哈希。

### 当前剩余问题的首个建档状态

保存的首个公开窗口中，前三个聚类分别对应主要说话人；第四个聚类的重复上下文中包含 `006-M` 12.682 秒、`004-M` 7.410 秒和 `001-M` 0.480 秒，却占用了第四个角色档案。后续一个包含 `003-M` 8.114 秒证据的聚类未获分配。这里的时长含重叠窗口的重复采样，不能当作实际会议发言时长。

这提供了“混合上下文档案占用角色名额”的明确状态线索，尚未通过同状态干预证明完整根因。它与输出片段串人的修复分开跟踪：本轮没有改角色上限、重建档案或用标注参与推理。[保存的建档诊断](evidence/meeting-speaker-fix-20260915/remaining-enrollment-diagnosis.json)。

### 已知窗口覆盖限制

新增双人会议 `S_R004S03C01` 的旧包基线，只有终场分人结果，原 `diarization-windows` 门禁为 FAIL。最后一个非终场 ASR 分句止于 90.3 秒，后续直到 finish 没有分句边界，因此未触发 120 秒后的中途窗口；这一输入没有覆盖中途窗口冻结场景。原失败报告不改为 PASS，也不修改载体断言。基线仍有唯一 last 后唯一 complete、无 error、finish 前 last=0、native stream=0；身份质量可以按完整终场时间线独立评估。

## 证据保存

- [综合评估](evidence/meeting-speaker-fix-20260915/assessment.json)：逐条精度、原始变号、生命周期、资源与未解决项。
- [原始证据哈希清单](evidence/meeting-speaker-fix-20260915/raw-evidence-manifest.json)：23 个本地文件，包括三组真机报告、逐轮结果、内存、hilog、输入映射及单测日志。
- [第一阶段构建身份](evidence/meeting-speaker-fix-20260915/bounded-build-identity.json)、[新增音频清单](evidence/meeting-speaker-fix-20260915/additional-input-manifest.json)。
- 6 秒原报告：`usb-bounded/20260915-150714-diarization-windows-7a1edaf8/report.json`，SHA-256 `fc4511048f1b97243c2cc3431783058a47da31774ba04af2c470d809500b3f23`。

原始目录：`~/.cache/amphion-runtime/diagnostics/meeting-speaker-fix-20260915-6f7oc5vh/`。保存原始与新增基线、修复结果、输入映射、逐轮回调、hilog、内存、构建身份、假设计划和被否决实验。原始 PCM、文本、embedding 和签名材料不加入 Git。

本次为问题专项验证，未覆盖 Android 真机、更多麦克风通道、客户原始录音/版本、长会话身份漂移、断网网络观测和完整发布矩阵；不构成正式交付或主线缺陷关闭依据。
