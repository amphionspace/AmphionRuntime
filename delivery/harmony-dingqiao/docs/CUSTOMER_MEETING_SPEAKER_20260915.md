# 客户录音：离线角色分离核对

**客户问题仍未解决。已定位并修复一处将可靠短句匹配结果丢弃的分支；本地测试与鸿蒙编译通过，新包真机验证等待调试签名刷新。** 这项修复能改善 86 秒录音中的一处身份未知，不改善 6 分钟录音后半段的大量未知身份。

## 来源与核对口径

通过 `lark-cli` 读取[客户问题文档](https://ccnuebbmik1f.feishu.cn/wiki/JaccwHakcit4Fnkk4IycN8UgnOE)，保存修订号 180 的正文、相关日志与录音附件。没有向飞书写入内容，也没有上传推理数据。

用户要求暂按正文的原 `u1/u3/u4` 为同一人核对。这是待确认的身份标注；当前 SDK 将录音分成 9 句，原日志为 7 句，需要按内容和音频时间对应，不能直接比较句子编号或不同版本的角色编号。没有完整身份标注，以下不计算客户录音 DER，也不把未知减少直接视为精度通过。

| 录音 | 实际时长 | 当前验证状态 |
| --- | ---: | --- |
| `smartFill-1789370115658.wav` | 86.08 秒 | 上一版完整真机回放；新修复已完成保存状态的对照 |
| `meetingMinutes-1789438186695.wav` | 374.16 秒 | 上一版完整真机回放；新修复逐段身份不变 |
| `smartFill-1789197567437.wav` | 3123.44 秒 | 三个 RAR 分卷已完整解压并验证 PCM 帧数，尚未回放 |

最后一条实际为 52 分 3.44 秒，“运行 20 分钟”是日志摘录范围。原始 PCM、日志、文本和声纹向量仅保存在本地诊断目录，不进入 Git。

## 客户录音的真机基线

运行代码为 `fd6211230a635c6b6b64fab20b0827aecb254718`，与四段会议上一轮验收的 HAP 相同。设备为 PSN-AL00 / OpenHarmony 6.0.2.130，中英 diagnostics 测试载体；两条均使用完整原始 PCM、20 ms 实时输入。

| 检查 | 86 秒录音 | 6 分钟录音 |
| --- | ---: | ---: |
| ASR 最终句子 | 9 | 34 |
| 身份未知句子 | 5 | 31 |
| 分人窗口数 | 1 | 3 |
| 核心生命周期 | PASS | PASS |
| finish 到 complete | 588 ms | 4548 ms |
| 分人推理 RTF | 0.294 | 0.343 |

两轮均为 start=1、finish 前 last=0、唯一 last 后唯一 complete、error=0、native stream=0。身份时间区间与宿主机相同输入的生产会话结果逐段一致。

原始 `diarization-windows` 总报告仍为 FAIL：86 秒不足以覆盖该模式要求的中途窗口。保留原断言和失败报告；6 分钟录音虽然窗口/生命周期检查通过，身份精度并未通过。

整个进程 RSS 增长 54.871 MiB、峰值 804.320 MiB，通用 64 MiB 检查通过。这两轮结果不足以证明 52 分钟会议的稳定内存预算，更不能抵消此前四条连续会议的原始资源 FAIL。

## 第三项局部修复：允许未知片段核对已有角色

提交：`4c6c45a47fdc30e9948b34ef472a5b06a7e7c252`。

**第一个错误状态在会话提交阶段。** 原正文 u4 对应当前约 46.12–48.56 秒的句子。输出区真实单人 PCM 的声纹与原 u1 的已建立档案相似度为 0.685240，次高为 0.245554，满足现有 0.59 门槛及 0.05 差值。上下文聚类却将该片段归为 UNKNOWN，随后代码因 UNKNOWN 提前跳过短句核对，丢弃了这份合格证据。

- 改变：Harmony 和 Android 删除这一个 UNKNOWN 提前跳过条件，允许当前片段匹配已公开的角色。
- 保持：查询门槛 0.59、差值 0.05、建档/聚类门槛 0.72；至少 1 秒当前输出区内真实单人 PCM；候选只含已有公开角色；查询不创建或修改档案；已发布结果冻结；PCM 归属及回调契约。
- 不处理：角色上限、建档策略、模型替换、重叠语音解混、ASR 精度及正式交付。

固定 native 分割、声纹向量、句子和公开窗口边界，仅改变这个分支，结果如下。表中编号均为当前版本的编号。

| 正文对应句 | 当前时间 | 修复前角色 | 保存状态对照结果 |
| --- | --- | ---: | ---: |
| 原 u1 | 2.72–3.96、6.32–7.80 秒 | 2 | 2 |
| 原 u3 | 38.32–40.56 秒 | 未知 | 未知 |
| 原 u4 | 46.12–48.56 秒 | 未知 | 2 |

原 u3 的短句查询相似度只有 0.524265，仍未达到门槛；没有为本录音降低门槛。整体未知句子在保存状态对照中从 5 变为 4，其余 8 句身份不变。此前四段有身份标注的会议，逐段身份、DER、混淆时长、变号次数及档案均不变。

验证进展：

- 实际生产会话回归先失败：合格查询仍返回未知；删除该分支后通过。
- Harmony 查询相关 5 项检查通过，保护弱匹配保留未知、不更新档案、不引入仅在上下文出现的角色、严格 PCM 区间及公开结果冻结。
- Android 基础/鼎桥 SDK 的 Debug、Release 单测通过。
- 复用正式脚本的隔离源码准备流程，鸿蒙生产 ArkTS / HAR 编译通过。这个未安装的 HAR 只作为编译证据，不作为真机验收或交付件。
- 已安装 HAP 仍为 `fd621123`，不包含此修复。调试证书于本日 **16:44:58** 过期；已请求刷新同应用调试签名。没有绕过签名校验。恢复后只需先用新 HAP 回放 86 秒原音，核对这一处变化和生命周期。

## 剩余问题与证据

### 6 分钟录音后半段仍大量未知

第二、第三个窗口分别有 12/12、9/9 句未知。这不是单一的输出编号问题：

| 保存的内部状态 | 第二窗口 | 第三窗口 |
| --- | ---: | ---: |
| 完全没有角色分离语音区间覆盖的 ASR 句子 | 8 | 2 |
| 已提取的上下文声纹数 | 17 | 56 |
| 与已有角色匹配成功的上下文数 | 0 | 0 |
| 输出短句查询的最高相似度 | 0.507608 | 0.401498 |
| 合格短句查询数 | 0 | 0 |

四个角色档案最后更新都停在首窗口 122.980 秒。现有证据区分了语音区间缺失与身份匹配失败，但尚未证明其完整声学根因。此次删除查询跳过条件不能改变这些结果。不得用增加编号、复制上一句角色或降低阈值掩盖。

“第四个档案完全来自上下文、从未实际发声”的假设已否决：首窗口四个角色都有公开语音区间，分别为 6.238、9.715、6.844、5.121 秒。这不证明它们对应四个自然人，也不能据此引入档案过滤。

### 其他未解决项

- 86 秒录音约 38 秒的同人短句仍为未知；后段存在 ASR 有文字而角色分割未检出语音的区域，独立跟踪，不归因为本次分支错误。
- 52 分钟录音尚未回放。原日志终场带降级标志、应用 15 秒等待期限和完整录音的后段窗口已保存；没有把日志片段当成当前提交验收。
- 客户身份标注仍为暂定；整体身份稳定性和长会议资源结论均未通过。

[客户真机基线及输入哈希](evidence/meeting-speaker-fix-20260915/customer-device-baseline.json)；[局部修复、红绿测试、状态差分和原始证据哈希](evidence/meeting-speaker-fix-20260915/customer-unknown-query-assessment.json)。前两项修复与四段会议结果见[会议修复报告](MEETING_SPEAKER_FIX_20260915.md)。
