# 单人额外身份：修复候选与退化检查（2026-09-16）

## 结论

候选提交 `95b8f14c` 修正了这次录音结束后误留的第二个身份，但不代表所有单人分裂和多人身份漂移已经解决，也未通过完整发布验收。

这是一项保守取舍：证据不足的额外身份返回 `UNKNOWN`。真实的新说话人如果有效语音不足，也可能较晚出现编号或最终保持未知。不能只用“角色数量变少”判断成功。

## 现场和第一个错误决定

- 用户确认整段只有一人；过程中看到三个编号，结束后剩一条错误的“说话人 2”。
- 原包为 `9ced6004`，当时参数是会议纪要、MIC、角色分离开启、`vadEnd=800 ms`。
- 普通导出截断在 55 秒；本次从 SDK 诊断目录找到了完整 **69.56 秒**输入，未截断。
- 错误身份对应约 **38.927–40.091 秒**。该声学片段约 1.16 秒，与主身份中心的相似度约 0.53，被原策略当作陌生人。
- 同一短片段进入四个重叠窗口，聚类累计权重约 4.72 秒。这个权重不能证明存在 4.72 秒独立语音，更不能替代新身份所需的充分证据。
- 主机固定声学窗口经过生产会话代码后，复现了最终两个身份以及同一错误时间段。不是通过强制人数为一来“通过”。

## 改变和保持的行为

新增第二个及后续身份，要求至少一个分析窗口包含该候选不少于 **3 秒有效语音**。窗口长 10 秒，语音可由多段组成，不要求一句连续说满 3 秒。最终确认检查单次观察，不把重复窗口的时长相加作为准入依据。

保留首个身份和已知身份的短语音匹配；初次最终聚类优先使用证据充分的候选，避免重复短片段仅凭累计权重抢占初始身份。相似度阈值、四人上限、ASR 文本、`vadEnd`、生命周期和发布窗口边界均未修改。Harmony 与 Android 同步实现。

明确未解决：录音中的临时编号分裂、真实重叠下的身份精度、所有短句的正确归属、快速喂入的结束异常、长会议资源验收。3 秒是保守工程策略，不能保证达到该时长就一定认对人。

## 已否决的方案

“所有新身份（包括第一个）都统一等待 3 秒”在固定输入差分中产生退化：

- 4 人片段实时误认：13.67 → **15.12 秒**。
- 2 人片段实时未知：1.47 → **3.47 秒**。

只在最终阶段加门槛的变体也不采用：3 人片段实时误认由 5.05 增至 **10.91 秒**。这些实验保存在本地，均为非交付证据。

## 保留候选的固定声学状态差分

每段 180 秒。下表“最终”是整段窗口提交结果；“实时”是尚可被修正的过程结果。它们来自相同声学窗口的生产状态机差分，不能替代真机全链路验收。

| 人数 / 片段 | 实时误认秒：原版 → 候选 | 实时未知秒：原版 → 候选 | 最终误认秒 | 最终未知秒 |
|---|---:|---:|---:|---:|
| 2 / S_R003S01C01 | 16.37 → 15.94 | 1.47 → 2.47 | 4.13，不变 | 1.47，不变 |
| 3 / M_R003S02C01 | 5.05 → 5.05 | 27.96 → 24.45 | 0.90，不变 | 4.98，不变 |
| 4 / L_R003S01C02 | 13.67 → 11.76 | 129.98 → 127.27 | 19.23，不变 | 60.41，不变 |

2 人片段多约 1 秒临时未知，属于实际代价。3 人片段过程中仍可能出现第四个临时编号，最终为三个；按用户允许纠正性跳变的口径单独披露，不仅凭编号切换次数判失败。4 人片段原有误认和未知比例仍高，精度问题保持 OPEN。

旧 47.26 秒单人样本最终仍全部为同一角色、无未知。新 69.56 秒样本最终为一个已知角色，未知声学时长约 1.432 秒；不能表述为“全文正确归到同一个人”。

## 回归测试

Harmony 41 项、Android 鼎桥 SDK 106 项通过。新增可失败用例同时检查：

- 重复四次同一 1.2 秒片段，不得创建额外身份；
- 单个短录音的首个身份仍能建立；短语音仍能匹配已知身份；
- 新身份 2999 / 3000 ms 边界；证据充分的不同人仍可区分；
- 重复短片段不能凭累计权重抢占首次聚类身份；
- 保留真实重叠的双身份信息、窗口提交边界和公开回调顺序。

原有重叠用例的输入调整为满足新增身份条件的真实长度，保留原断言；原窗口测试的模拟 embedding 时长更新为当前生产提取器使用的最多 6 秒上下文。新增短句用例明确覆盖门槛造成的未知，不能把这一行为藏在调整后的旧用例中。

## 真机快速喂入对照：失败，不能作为发布通过证据

同一设备、三条相同 PCM、相同次序、每帧不等待、相同 SDK 结束等待门槛：

| 项目 | 原包 9ced6004 | 候选 95b8f14c |
|---|---|---|
| 新单人录音 | 正常完成；最终误留第二身份 | 正常完成；一个已知角色，14 / 146 个文本字符未知 |
| 旧单人录音 | 正常完成 | 正常完成；92 字全为同一角色，无未知 |
| 4 人片段 | 无 complete，快照 native stream=2 | 同样无 complete，快照 native stream=2 |
| 首尾 RSS 增长 | 60.250 MiB | 68.691 MiB，超过 64 MiB 门槛 |
| RSS 峰值 | 792.957 MiB | 783.164 MiB |

结束异常在旧包同条件出现，属于已有问题；具体队列/释放根因仍需独立定位。候选 RSS 门槛失败保留；一次对照不足以把 8.44 MiB 差异归因于改动或证明无资源退化。不得更改门槛后改记 PASS。4 人没有完整结果，本轮精度只能记 INCONCLUSIVE，不能拿空输出的“零误认秒”宣称改善。

## 构建与证据

- 运行代码：`95b8f14c5cb01a22a21ddb17b0d703434ca709d2`，仅 ZH_EN 测试 HAP，SDK 0.3.13 / Demo 0.3.14，沿用原授权和签名。
- 候选 HAP SHA-256：`6426919144e11c685156da762e00211afe6aee40332b0967889aaa523ae4131d`。
- 原包 HAP SHA-256：`19e8e323f8895c46dc90acbe0eef443b594b565d0ac9ee121b3a149c626eb534`。
- 原始 PCM、逐句结果、完整日志和失败现场仅保存在本地诊断目录，不提交原始录音或 embedding。
- 本地目录：`~/.cache/amphion-runtime/diagnostics/single-speaker-three-20260916-rr8vvcny/`。
- [脱敏结果汇总](evidence/single-speaker-enrollment-20260916/assessment.json)与[本地原始证据哈希清单](evidence/single-speaker-enrollment-20260916/artifact-manifest.json)。

## 真机实时喂入：限定范围通过

使用相同候选 HAP，每帧等待 20 ms，重放新单人完整录音与 4 人片段。SDK 结束等待门槛未调整。

- 新单人：最终一个已知角色，声学未知 1.432 秒；146 个转写字符中 14 个未知。临时编号仍出现 0、1。停止后 1165 ms 完成。修正的是“误留另一个身份”，尚未把该短句正确归回本人。
- 4 人：最终四个角色，误认 19.23 秒、未知 60.41 秒，与修复前固定声学结果一致；停止后 3851 ms 完成。只证明本片段未增加最终误认，原有精度仍不合格。
- 两例均无提前 last、结束后唯一 last 再唯一 complete、无 error/degraded、native stream 归零，逐句显示文本与 SDK 转写一致。
- 本组 RSS 增长 43.186 MiB，峰值 750.684 MiB，当前资源门槛通过；不能覆盖长会议，也不取消快速喂入时的资源疑点。

原始总报告保留 **FAIL**：我误用了含三个输入的尾句清单，而本组只执行两个输入。两个实际执行文件的尾句断言原本均 PASS；汇总失败是缺少未执行的旧单人文件。对原封不动的回调结果按实际两个输入重新核对，结果 PASS，见本地 `paced-tail-reassessment.json`。未放宽文本断言、未覆盖原报告，也未为这个清单错误重复播放音频。

手机保留 `95b8f14c` 候选测试包，恢复为 MIC、会议纪要、角色分离开启、`vadEnd=800 ms`，可继续现场验证。候选不等于正式交付；快速喂入、资源归因和多人总体精度仍 OPEN。
