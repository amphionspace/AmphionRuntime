# 中文交付邮件默认模板

沿用 0.3.13 邮件的称呼、版本结论、中文编号更新项、兼容性、验收结果、交付清单和收尾。主题和正文中的占位内容必须按本次事实替换；更新项数量可调整，保持连续编号。

先从上一交付提交到本次构建提交逐项核对 PR。需要业务方迁移的变化放在首项；没有此类变化时删除对应提示。不要固定复制旧版本的能力范围、测试轮数、耗时或 PASS。区分 ASR 生命周期兼容与专项回调迁移，不能统一写成“所有接口不变”。

---

主题：HarmonyOS ASR SDK <本次版本> 交付及更新说明

各位好，

本次交付版本为 **HarmonyOS ASR SDK <本次版本>**，请以本次提供的完整交付包为准。

相对 <上一交付版本>，本次更新主要包括以下内容。<需要业务方同步调整的接入提示；没有则删除。>

**一、<主要更新及接入变化>**

<说明具体场景下的前后行为。涉及结果范围、回调次数、批次累计或终结标志变化时，列出调用方需要调整的字段和处理方式。>

**二、<其他功能修复或优化>**

<按实际 PR 填写用户可见的变化，不把实验选项描述为已验证的性能收益。其他更新项依次编号。>

**三、ASR 生命周期与接入兼容性**

<根据本次实际变更说明保持不变及需要迁移的部分。确认无变化时可采用以下内容：>

- `onStart` 表示 session 已可用，可立即调用 `writeAudio`、`finish` 或 `cancel`。
- `isFinal` 表示一句话或 endpoint 的最终结果；`isLast` 只表示整个 session 的最后一条结果。
- 普通连续识别由调用方最终执行 `finish(sessionId)`。
- 正常结束保证唯一一次 `isLast=true`，随后唯一一次 `onComplete`。

建议继续采用现有流程：启动录音并缓存 PCM，调用 `startListening()`，收到 `onStart` 后按顺序回灌缓存，再进入实时写入。

**四、最终 ZIP 验收结果**

本次使用最终 ZIP 中的实际交付内容进行验证：

- 解压安装包内 Diagnostics Demo：<用例、轮数、实际结果>。
- 使用包内 Demo 源码和正式 HAR，补入本地授权及签名后独立构建、安装：<用例、轮数、实际结果>。
- <新增/变更能力专项的输入条件、覆盖行为、实际结果>。
- <重要限制、未覆盖条件、失败或 INCONCLUSIVE 指标及报告位置；不能省略影响交付结论的问题>。

<只有实际通过后才能写通过；未运行专项不得用其他模式的通过结果替代。快速专项不等于精度、长期稳定性或离线网络观测。>

本次交付包括：

- 正式 Release SDK；
- Debug/Diagnostics SDK；
- 已签名 Diagnostics Demo；
- 可独立构建的 Demo 源码；
- 验收摘要及构建来源信息；
- SHA-256 校验清单。

请以 **HarmonyOS ASR SDK <本次版本> 完整交付包**及配套验收报告为准。
