# Android ASR 0.3.8 最终 ZIP 验收报告

结论：本次约定范围可以交付；通用角色身份准确率仍有阻断项。

- 日期：2026-09-18
- 交付分支：`chore/asr-delivery-20260918`
- 主线基础：`57437cec8b2d2ffd76cba53bac99a71235d754ae`（PR #214 已合入）
- 构建源码：`5f622c6d390e2088570c5f9bbdc212f2f702f3a4`；后续版本断言、台账及证据提交不改变二进制。
- 最终 ZIP：`Amphion-Android-ASR-Complete-0.3.8.zip`
- SHA-256：`00b1f760a31cf1e490b1a3776db0c939027a9549c2183e66c385ab2eeaa9141d`
- 字节数：1968780977

## 交付物与接口验收

设备：TECNO KI8，Android 13，arm64。使用最终 ZIP 的正式 AAR 与独立源码构建测试载体。

- Debug／Release 单元测试共 373 项通过。
- 真机 50 项通过；首轮 48 项通过、2 项因缺少外部测试语料未进入断言，补齐既有语料后只重跑这 2 项并通过。原失败日志保留，没有修改断言或 SDK 运行代码。
- 300 轮用户操作压力测试通过，完成 600 个正常 session，同时覆盖取消、迟到调用、回调重入及恢复。
- 最终 ZIP 的 Diagnostics APK：168.2 秒客户录音，28 个完整句、2 个角色窗口，文字守恒、多人句不强行归给单人、last／complete 顺序通过；但角色处理发生降级，仅有角色 1／2 的确定句，没有角色 4，角色专项 FAIL。降级原因未被该测试序列化，不能把 10 秒单窗 watchdog 的可能影响写成已证实根因；模型哈希已核对一致。
- 最终 ZIP 的 R8 Release APK：实际界面先显示中间结果，随后定稿；28 个完整句与同次正式 SDK 的 ASR final 逐句一致，角色 4 对应已确认的播放声音（8.42–15.74 秒），能够再次启动。包内 128 项文件校验通过。不能拿已降级 Diagnostics 的角色标签充当正式版基准；原比较 FAIL 已保留。
- 168.2 秒音频从记录开始到完成为 186.716 秒，包含启动、输入和结束处理，未隔离纯 finish 延迟。轮询中未观察到正式界面的降级提示，但未记录正式版窗口 degradedReason，不能据此严格宣称“无降级”。

Diagnostics 角色能力及其与 Release 的一致性在本机型上仍为阻断项；不能宣称两个构建的角色能力全部通过。正式 Release 的句级与角色 4 收益成立，按用户约定范围交付；本轮不扩展算法、不延长 watchdog。没有旧版本在同设备上的差分证据，不能把这个问题认定为本轮引入的回退。

测试采用前台运行条件，不将设备防后台挂起的测试载体视为 SDK 后台能力修复。设备时钟与主机不同；回调证据使用已保存的追加快照边界归属，未用主机时间误删回调。实际 UI 和客户公开结果保留在私有诊断目录，公开证据保留校验值和结论。

## 客户收益、迁移与剩余风险

本轮按已约定的客户音频收益交付。角色 4 已恢复，句级结果保留完整文字，多人或重叠句保留“不确定”。通用角色身份精度仍未通过：既有 2／3／4 人控制样例仍有 0.82／0.05／13.86 秒身份误认，部分主讲未知及嘈杂插话问题保留；这些仍是通用精度验收的阻断项，本次没有宣称全部解决。

调用方须按 `sourceUtteranceId` 对应原 ASR final，保留完整句。`speakerIndex=-1` 表示多人或不确定，`secondarySpeakerIndexes` 本身不能证明重叠；使用定稿 `overlap` 及原始 `speakerTurns`。`speakerInferred=true` 的邻接推断仍为 `confidence=0`。按 `windowIndex` 累积角色结果，已提交窗口冻结；角色末批使用 `isSessionFinal`，不可与 ASR 的 `isLast` 混用。

普通识别的 `onStart` 可同步写入、结束或取消；显式结束前无 last，正常结束为唯一 last 后唯一 complete。当前发布门禁未发现这些契约的新失败。未新增算法或模型实验；本轮主要验证范围内的客户收益和相邻生命周期，不能据此声称所有录音、机型与场景无回退。

短时资源观测不能证明长会议内存稳定。没有新增全量断网抓包、后台锁屏或外部故障注入证据。MOSS 仅用于研发标注，SDK 推理在端侧；本包不含 TTS 和目标说话人增强模型。五条客户离线录音及标注已存入私有 OBS 验收数据，云端识别录音未纳入。客户 PCM、转写和 embedding 不进入公开 Git 证据。

## 变更对照及证据

相对 Android 0.3.7／Harmony 0.3.16，功能变化来自 PR #214；PR #213 为上一交付台账与证据。本分支仅更新版本、日期、包内模型完整性检查、迁移文档及过期测试预期。

发布证据位于仓库对应平台 `evidence/release-gate/20260918-5f622c6d-0.3.8`。客户原始证据留在私有诊断目录，受控范围及历史精度对照见 `CUSTOMER_MEETING_SENTENCE_WRAPUP_20260918.md`。报告绑定上述 ZIP，未回填 ZIP 改变已验收哈希。
