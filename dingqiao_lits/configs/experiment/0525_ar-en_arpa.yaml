# @package _global_

defaults:
  - override /data: ar-en_16k

model:
  n_vocab: 156
  optimizer:
    lr: 2e-4

tags: ["ar-en", "ar_char", "en_arpa", "16k"]
name: ar-en_bs${data.batch_size}_lr${model.optimizer.lr}
run_name: ${name}_${now:%Y%m%d_%H%M%S}
