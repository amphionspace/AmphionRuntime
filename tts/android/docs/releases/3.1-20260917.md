# Android TTS 3.1 交付记录

日期：2026-09-17。产物文件名、大小、SHA-256、AAR 来源提交及模型哈希见 [结构化记录](3.1-20260917.json)。

## 内容

- ZIP：lits-dingqiao-tts-android-sdk-vocos24k-3.1.zip。
- AAR：lits-dingqiao-tts-sdk-vocos24k-3.1.aar；包含 ONNX Runtime Java 和 arm64 原生库。
- 外置模型 IMF 170000（checkpoint step_step=00169999.ckpt），配套 Vocos、173 token 前端；FP32、24 kHz、speaker 1、温度 0、两步、默认 50 帧。
- 沿用 Gradle stageSdkDelivery 的结构及原有 README / CHANGELOG / LICENSE / NOTICE、API / INTEGRATION / DELIVERY / PSEUDOCODE 文档；原 Kotlin 接入示例保留在文档内，11 段已编译通过。随包 CHECKSUMS.txt 为当前逐文件 SHA-256。
- 不附授权文件、私钥、SN 清单、测试 APK、validation 目录及内部 BUILD_INFO。模型目录保留原始完整资源和 manifest。
- 授权兼容主版本仍为 sdkMajor=1；交付版本 3.1 不修改授权兼容条件。

## 验证与来源

本次由已验收 IMF 170000 包整理而来，AAR 与整套模型资源逐文件 SHA-256 一致，只调整交付版本、文件名、文档及内部报告的分发范围；原示例编译检查触发构建后，AAR 哈希仍与已验收产物相同。Gradle sdkVersion 更新为 3.1，不更改 license SDK_MAJOR。

复用相同 AAR、资源和 TECNO KI8 / Android 13 的验收证据：

- Release AAR 消费者接入和 原接入文档 Kotlin 示例编译通过；6 段中英文合成通过。
- 新签单机测试授权：6 段中英文合成和流式播放完成；错误 SN、sdkMajor=2、仅 ASR 权限、篡改签名均被正确拒绝。
- 数字专项：zh-en 的 1. / 一.、2. / 二. 音素及 PCM 一致；en-US 的 1. / one. 一致，7 例播放完成。
- 导出对齐：4 个条件编码用例、11 种解码长度，最大解码绝对误差 4.03e-5。
- 最终 3.1 ZIP 已解压内容校验（逐文件 SHA-256）、ZIP CRC 检查及敏感文件名检查。

内部原始证据保留在工作区 outputs/Amphion-TTS-Android-IMF170000-20260917/validation、outputs/current-phone-license-test-20260917、outputs/numeric-period-probe-20260917，不随客户包分发；这些目录是本地归档，不是 Git 内路径。

本次未重跑完整 API 矩阵、完整发音语料、1000 条长稳压或 RSS 上限验收。现有 PERMISSIVE 模式和宿主提供 SN 的授权设计未调整。

本记录仅表示本地组包及验证完成，不代表已上传或对外发布。

## 本次文档对齐

沿用旧示例结构；必要纠正为外置资源部署、24 kHz PCM、TtsLicenseOptions.enforcement 参数名、授权初始化顺序和当前实际混淆状态。DELIVERY.md 保留历史源码构建内容并明确其不适用于当前外置模型包。旧 shell 打包脚本不作为本次结构/命名依据。
