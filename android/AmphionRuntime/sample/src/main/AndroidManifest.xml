<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".AmphionApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AmphionAsrSample">

        <!-- LAUNCHER 入口：评估模式 vs Demo 二选一的着陆页 -->
        <activity
            android:name=".eval.LandingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 评估模式三个 Activity -->
        <activity
            android:name=".eval.EvalActivity"
            android:exported="false" />

        <activity
            android:name=".eval.RecordSentenceActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".eval.SentenceDetailActivity"
            android:exported="false" />

        <!-- 原 Demo Activity（保留，从 LandingActivity 跳转） -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- FileProvider：把 ZipExporter 写出的 zip 用 content:// URI 暴露给分享面板 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.eval.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/eval_file_paths" />
        </provider>

    </application>

</manifest>
